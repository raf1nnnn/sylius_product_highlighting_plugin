<div class="ui segment">
    <form method="get" action="{{ path('dotit_sylius_highlighting_plugin_shop_product_highlighting_index') }}" class="ui loadable form">
        <div class="ui stackable grid" id="searchbar">
            <div class="column" id="searchbarTextField">
                {% for filter in product_highlightings.definition.enabledFilters %}
                    {{ sylius_grid_render_filter(product_highlightings, filter) }}
                {% endfor %}
            </div>
            <div class="right aligned column" id="searchbarButtons">
                <div class="ui buttons">
                    <button type="submit" class="ui primary icon labeled button" {{ sylius_test_html_attribute('search') }}><i class="search icon"></i> {{ 'sylius.ui.search'|trans }}</button>
                    <a href="{{ path('dotit_sylius_highlighting_plugin_shop_product_highlighting_index') }}" class="ui negative icon labeled button" {{ sylius_test_html_attribute('clear') }}>
                        <i class="cancel icon"></i> {{ 'sylius.ui.clear'|trans }}
                    </a>
                </div>
            </div>
        </div>
    </form>
</div>
